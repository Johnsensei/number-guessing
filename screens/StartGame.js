import { View, TextInput, Button, Pressable } from "react-native";

export default StartGame = () => {
    return(
        <View>
            <TextInput />
            {/* BUTTONS */}
        </View>
    );
}